import React from 'react'

const Rules = () => {

    
  return (
    <div>
        {/* firebase security rules in this page uper part hai
        <p>rules_version="2"</p>
        <p>service cloud.firestore{</p>
        <p>match/databases/{database}/documents{</p>

        <p>match/users/{itemId}{}</p>
        <p>allow read: if request.auth.uid==resource.data.owner;</p>
        <p>allow create: if request.resource.data.title is string && request.resource.data.score is number</p>
        <p>allow update: if false;</p>
        <p>allow delete: if false;</p> */}


    </div>
  )
}

export default Rules